<block wx:if="{{$taroCompReady}}">
    <view class="authorIndex">
        <search compid="{{$compid__15}}"></search>
        <block wx:if="{{searchAuthorInfo.length > 0}}">
            <author search-content="search-content" compid="{{$compid__16}}"></author>
        </block>
        <view class="hot-wrap">
            <block wx:if="{{hotAuthor.length > 0}}">
                <view class="tips">著名作者</view>
            </block>
            <author compid="{{$compid__17}}"></author>
        </view>
    </view>
</block>